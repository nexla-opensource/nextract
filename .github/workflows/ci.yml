name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .[dev]

    - name: Lint
      run: |
        # Pinned minor: ruff 0.16 expanded its default rule set and flags
        # pre-existing modules; bump deliberately with a lint-cleanup pass.
        pip install "ruff>=0.15,<0.16"
        ruff check nextract

    - name: Run tests
      env:
        PYTHONWARNINGS: default
      run: |
        pytest tests/ --maxfail=1 -q
